global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "kuva-email-service"
    metrics_path: /metrics
    static_configs:
      - targets: ["kuva-email-service:8080"]
